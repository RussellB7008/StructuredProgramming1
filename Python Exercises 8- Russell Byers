Python 3.2.2 (default, Sep  4 2011, 09:51:08) [MSC v.1500 32 bit (Intel)] on win32
Type "copyright", "credits" or "license()" for more information.
>>> def exam_schedule(exam_date):
	"""In this function, I will convert the given tuple in (exam_date) into a list, and print out the list in a string with the completed sentence.
	"""
	if exam_date==tuple:
		exam_date(list)
	return ("The examination will start from : " + str(exam_date[0]) + "/" + str(exam_date[1]) + "/" + str(exam_date[2]))

>>> exam_st_date = (11, 12, 2014)
>>> exam_schedule(exam_st_date)
'The examination will start from : 11/12/2014'
>>> # The function uses the "if" operation and decides whether the given argument is a tuple or not. If it is a tuple, Python will convert it to a list and print it in the given format.
